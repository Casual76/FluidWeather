<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <application>
    <!--
      exported="true" e' obbligatorio: il broadcast di aggiornamento arriva dal launcher, che e'
      un'altra app. Il ricevitore si fonde in :app come gia' fanno quelli di core-cycle.
    -->
    <receiver
        android:name="dev.pampa.fluidweather.feature.appwidget.FluidWeatherAppWidgetReceiver"
        android:exported="true"
        android:label="@string/appwidget_label">
      <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
      </intent-filter>
      <meta-data
          android:name="android.appwidget.provider"
          android:resource="@xml/fluid_weather_appwidget_info" />
    </receiver>
  </application>
</manifest>
